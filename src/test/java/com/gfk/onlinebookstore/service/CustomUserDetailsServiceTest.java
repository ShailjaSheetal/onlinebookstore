package com.gfk.onlinebookstore.service;

import static org.assertj.core.api.Assertions.assertThat;

class CustomUserDetailsServiceTest {

    private static CustomUserDetailsService sut;

   /* @Test
    public void whenUserSignup_thenCreateUser() {
        User user = new User("user1", "user1", "uer1@aol.com", "123456", "010213544",);

        // Given
        given(userRepository.save(Mockito.any(ApiUser.class))).willReturn(user);

        ApiUser createdUser = userRepository.save(user);

        // Assertion
        assertThat(createdUser.getEmail()).isEqualTo(user.getEmail());
    }*/
}